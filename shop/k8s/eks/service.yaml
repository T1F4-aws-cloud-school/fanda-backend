apiVersion: v1
kind: Service
metadata:
  name: fanda-shop-service
  namespace: fanda-be-shop
  labels:
    app: fanda-shop
    service: shop
spec:
  type: LoadBalancer
  ports:
    - port: 8006
      targetPort: 8006
      protocol: TCP
      name: http
  selector:
    app: fanda-shop