apiVersion: v1
kind: Service
metadata:
  name: fanda-shop-service
  namespace: fanda-be-shop
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8006
      targetPort: 8006
      protocol: TCP
  selector:
    app: fanda-shop
