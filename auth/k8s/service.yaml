apiVersion: v1
kind: Service
metadata:
  name: fanda-auth-service
  namespace: fanda-auth
  labels:
    app: fanda-auth
    service: auth
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: fanda-auth