jwt:
  secret: ${JWT_SECRET}

# 서비스 URL들 (로컬 개발용 기본값, 프로덕션에서는 환경변수로 override)
services:
  auth-url: ${AUTH_SERVICE_URL:http://localhost:8002}
  shop-url: ${SHOP_SERVICE_URL:http://localhost:8006}
  feedback-url: ${FEEDBACK_SERVICE_URL:http://localhost:8003}